# DESIGN.md — 湯語教（ゆごきょう）対訳リーダー

江戸の銭湯経営手引書『湯語教／一名銭湯手引草』（嘉永4年・1851）を、
現代の人が読めるようにする対訳リーダー。史料サイトの堅さではなく、
**小杉湯原宿・小杉湯ストアのような「静かで親しみやすい銭湯カルチャー」のトーン**で作る。

## 1. Visual Theme

- プロファイル: docs / media（読み物）
- ムード: 余白たっぷり・静か・丸みのある親しみやすさ。ミルク風呂の淡さ
- 参考: 小杉湯原宿 https://kosugiyu-harajuku.jp/（生成りの背景＋丸ゴシック青ロゴ＋淡いブルー写真）
- コンテンツ言語: 日本語中心（古文＋現代語）
- 対象端末: **モバイル基準（375px）で書き、リーダーだけ2つのレイアウトを持つ**
  - 狭い画面（< `lg`）: 原本写真を画面上部に固定し、その下を本文が流れる
  - 広い画面（≥ `lg`）: 左に本文、右に原本写真を固定する2カラム
  - どちらも「写真は常に見えていて、本文は途切れない」という同じ体験を、縦と横に置き換えたもの
- ダークモード: なし（ライトのみ）

## 2. Color Tokens

Tailwind config で定義し、hex 直書き禁止。

| token | hex | 用途 |
|---|---|---|
| `paper` | `#F6F4EF` | ページ背景（生成り） |
| `paper-warm` | `#EFEBE2` | セクション背景・カード |
| `ink` | `#33302B` | 本文テキスト（墨色）。**現代語訳はこれ** |
| `ink-muted` | `#5A554C` | **原文翻刻の本文**。`ink` より退き、`ink-soft` より読める中間層（paper上 6.7:1） |
| `ink-soft` | `#736E64` | 補助テキスト・キャプション・ルビ |
| `yu-blue` | `#3D6EB4` | ブランド・見出し・リンク・アクセント（小杉湯原宿の丸ゴ青） |
| `yu-blue-soft` | `#DCE6F2` | ボタンの hover 面**のみ**。本文を囲む面としては使わない |
| `kuchinashi` | `#E8A63D` | ごく控えめな強調 ※paper上 1.92:1 なのでテキスト・罫には使わない |
| `kuchinashi-deep` | `#B2761A` | 未確定箇所の下線（paper上 3.47:1。非テキストUIの3:1を満たす） |
| `line` | `#DDD8CC` | 罫線 |

- **主役は現代語訳。** 訳は地の文として、面も枠も持たず `paper` の上に `ink` で直接置く
- 原文翻刻は**引用**として従に置く。面（塗り）で層を分けるのではなく、
  **左罫（`line`）・書体（`genbun`）・文字色（`ink-muted`）・文字サイズ**の4つで従位を示す
- **淡青の箱で訳を囲まない。** 読者がいちばん読みたいテキストが「注釈の箱」に見えてしまう
- `paper-warm` の面は挿絵解説など「本文ではないもの」にだけ使う。
  `paper-warm` 上のテキストは `ink`（`ink-soft` は 4.26:1 で不足）
- 白 `#FFFFFF` はカード内面にのみ使用可

## 3. Typography

- 見出し・ロゴ的要素: `"Zen Maru Gothic"`（丸ゴシック、Google Fonts）— 小杉湯原宿の丸ゴ感
- 本文（現代語・UI）: `"Zen Kaku Gothic New", system-ui, sans-serif`
- 翻刻（古文）: `"Shippori Mincho", serif` — 原文であることを書体で示す
- **サイズは名前付きトークンだけを使う。`text-[15px]` のような arbitrary 値は禁止**

| token | px | 行間 | 用途 |
|---|---|---|---|
| `text-yaku` | 17 | 1.9 | **現代語訳＝地の文**。このサイトでいちばん大きい本文 |
| `text-genbun` | 15 | **2.5** | 原文翻刻 |
| `text-lede` | 15 | 2.0 | Home / About の導入文 |
| `text-caption` | 13 | 1.7 | キャプション・注記・丁マーカー |
| `text-rubi` | 10 | 1.0 | `<rt>` |

- **行間1.9という原則の例外はルビ付きの行だけ。** `<rt>` は行ボックスの外に描かれるため、
  1.9 のままでは前の行と重なる。原文の行は 2.5 とする
- 数字は `tabular-nums`。半角英数字のみ

## 4. Component States

- リンク/ボタン: default=`yu-blue` / hover=10%濃く / active=さらに濃く / focus-visible=`ring-2 ring-yu-blue ring-offset-2` / disabled=40%opacity
- タップ領域は最小 44px
- **折りたたみ（注記）**: ネイティブの `<details>/<summary>` を使う。
  `<summary>` は `min-h-11`、マーカーは消し、ラベルは開閉で文字を差し替える（`注をひらく` ⇄ `注をとじる`）。
  状態はブラウザが持つので ARIA を手で足さない。focus-visible は共通のリング
- `transition-colors` 等プロパティ明示。`transition-all` 禁止

## 5. Layout

- コンテンツ最大幅 `max-w-2xl`（読み物）。**リーダーだけ `max-w-5xl` の2カラム**（§6）
- 余白スケール: 4/8px系。セクション間は `py-12`（モバイル）〜 `py-20`
- 角丸: カード `rounded-2xl`、ボタン `rounded-full`（丸ゴの丸みと揃える）
- 影は使わない（罫線と面の色で区切る）。装飾は引き算

## 6. リーダー画面の構造（このサイトの心臓部）

**区切りは「丁（見開き）」ではなく「章（内容のまとまり）」。** 原本の章境界は紙の途中に来るため、
丁単位で区切ると話の途中でページが終わる。章定義は `src/lib/chapters.ts`（corpus の上に載せる索引）。

1. 章ヘッダ: `第N章 / 全M章 ・ ◯〜◯丁 ・ AI翻刻（未校正）`（`ink-soft`・`tabular-nums`）
   → 章タイトル（丸ゴ・`yu-blue`）→ 要約
2. **原本写真は追随する固定パネルに置く。** 本文には挟まない。
   読んでいる丁に合わせて写真が切り替わり、本文は一度も途切れない。
   狭い画面では上部に固定、広い画面では右カラムに固定（§1）。
   写真は下端の定規・ラベル帯をトリミングして見せ、**拡大表示ではトリミングせず全体**を出す
   - **広い画面では見開きを割って1ページずつ出す。** 縦長のパネルにページが丸ごと入るので、
     1ページあたりの表示が約2倍になり、くずし字が読める大きさになる
   - **狭い画面は見開きのまま。** 縦の余地がないので、割ると横幅が痩せて損をする
   - どの段落が右ページ／左ページかは corpus に記載がないため、丁の中の位置から推定する
     （日本の本なので前半＝右、後半＝左）。外したときのために右⇄左の切替も置く
3. 本文は「丁マーカー」と「段落」が交互に並ぶ列:
   - **丁マーカー** — `◯丁` ラベル＋`h-px bg-line` の細い罫線だけの軽い区切り。
     写真はパネルに出ているので、ここには置かない。アンカー着地の基準点になる
   - **段落** — **①現代語訳（地の文・`text-yaku`・面なし）→ ②原文翻刻（左罫＋`text-genbun`＋`ink-muted`、ルビ付き）
     → ③注記（既定で畳む）** の順。読者が最初に目を通すのは訳。原文は「いま読んだ文の出どころ」として下に控える
   - **見出し** — `border-t border-line pt-8` を持たせ、章の中のまとまりの切れ目を見せる
   - **挿絵** — `paper-warm` の面にキャプション。**挿絵の注記は畳まない**（それが唯一の中身のため）
4. 丁が章をまたぐ場合（2 / 14 / 22 / 25 / 30丁）は、その丁マーカーに「前の章の続きから始まります」を、
   章末に「この丁の続きは次の章にあります」を一行添える
5. 前後ナビ（フッター固定、`← 前の章 / 次の章 →`）＋現在位置（`第4章 / 全11章`）。最終章は `読了 →`
6. 未確定箇所は `kuchinashi-deep` の**点線**下線で明示（AI翻刻・未校正の誠実な表示）。
   原文は従位置に退くが、AI翻刻であることの表示は退かせない
7. 章末に出典表記を1ブロック置く（リーダーではサイト共通フッターが隠れるため）

目次（Home）も章単位。各章に原本のサムネイルを添え、丁の総数（全38丁）は章数と併記する。

## 7. Do's / Don'ts

- Do: 余白で静けさを作る。1画面1目的。出典表記を常にフッターに
- Do: 「AI翻刻・未校正」を透明に表示する
- Don't: 和風クリシェ（和柄背景・筆文字・障子パターン）を足さない
- Don't: 絵文字をUIに使わない。装飾アイコンを増やさない
- Don't: hex直書き・arbitrary値・`transition-all`

## 8. ルビ（振り仮名）の扱い

- corpus の原文は `漢字（かな）` の形で読みを持つ。**表示時に本物の `<ruby>` へ変換する**。
  括弧のまま出すと原文の38.6%が括弧になり、対句が折り返してリズムが壊れる
- ルビ判定は「直前が漢字 かつ 括弧の中身が平仮名（＋長音符）のみ」。
  それ以外の括弧（`（此内佃島とも）` `（署名）` 等）は素の括弧のまま残す
- **ルビを振る漢字列は「直前の連続する漢字列」。例外は `src/lib/ruby.ts` の `RUBY_BASE_OVERRIDES` に列挙する**
  （`四十五代聖武皇帝（…）` の親文字は `聖武皇帝`）。推測ルールでは解けないことが実測で確認済み
- `<rt>` は `user-select: none`。本文を選択・コピーしたときに読みが混ざらないようにする
- `<rp>` は置かない（対象ブラウザはすべて ruby 対応。括弧がコピー文字列に混入するリスクのほうが大きい）
- 原文の行では `palt`（プロポーショナル詰め）を切る。ルビの位置合わせが崩れるため

## 8.5 原本画像の派生

`npm run make:derivatives` で表示用の縮小・切り出しを作り、生成物はコミットする（CIでは作らない）。

| 置き場所 | 中身 | 用途 |
|---|---|---|
| `public/koma/koma-XX.jpg` | 原寸 1600×1383 | 拡大表示のみ |
| `public/koma/w960/` | 幅960に縮小 | 狭い画面のパネル |
| `public/koma/w320/` | 幅320に縮小 | 目次のサムネイル |
| `public/koma/page/koma-XX-{r,l}.jpg` | 片ページ 700×1090 | 広い画面のパネル |

切り出しの矩形と「割らない丁」は `src/lib/pages.ts` が持つ。
表紙(1)・裏表紙(38)は本が左半分だけの別構図、8丁は見開き全面の一枚絵、37丁はほぼ白紙なので割らない。

## 9. Agent Prompt Guide

- references: 小杉湯原宿（kosugiyu-harajuku.jp）のスクリーンショット確認済み（2026-08-29）。
  生成り背景 `#F5F5F3` 近似＋丸ゴシック青 `#3D6EB4` 近似＋淡いブルー写真＋極小ナビ、という構成
- ユーザー指示: 「現代の人にわかりやすい形。小杉湯原宿とか小杉湯ストアみたいな雰囲気」「モバイル重点」
  「見開き一枚ずつだと読みづらい、内容で区切ってほしい」「写真が右で左は文章、スクロールで右の写真が切り替わる」
